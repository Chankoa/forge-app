"use client";

import { useState, useTransition } from "react";
import { Save } from "lucide-react";
import type { CourseLesson } from "@/lib/courses/contracts";
import { Button } from "@/components/ui/Button";
import { saveLessonAction } from "@/app/app/create/actions";

export function LessonEditor({ courseId, lesson }: { courseId: string; lesson: CourseLesson }) { const [message, setMessage] = useState(""); const [tab, setTab] = useState<"info" | "content" | "resources">("info"); const [pending, startTransition] = useTransition(); return <form className="form authoring-form" action={(data) => startTransition(async () => { try { await saveLessonAction(courseId, lesson.id, data); setMessage("Leçon sauvegardée."); } catch (error) { setMessage(error instanceof Error ? error.message : "La sauvegarde a échoué."); } })}><p className="caption">Leçon sélectionnée</p><h2>{lesson.title}</h2><div className="editor-tabs" role="tablist" aria-label="Édition de la leçon"><button type="button" role="tab" aria-selected={tab === "info"} onClick={() => setTab("info")}>Informations</button><button type="button" role="tab" aria-selected={tab === "content"} onClick={() => setTab("content")}>Contenu</button><button type="button" role="tab" aria-selected={tab === "resources"} onClick={() => setTab("resources")}>Ressources</button></div>{tab === "info" && <><label>Titre<input name="title" defaultValue={lesson.title} required /></label><label>Résumé<textarea name="description" defaultValue={lesson.description ?? ""} /></label><label>Type<select name="type" defaultValue={lesson.contentType}><option value="reading">Lecture</option><option value="video">Vidéo</option><option value="exercise">Exercice</option></select></label><label>Durée (minutes)<input name="durationMinutes" type="number" min="0" defaultValue={lesson.durationMinutes ?? ""} /></label><label>Statut<select name="publishingStatus" defaultValue={lesson.publishingStatus}><option value="draft">Brouillon</option><option value="published">Publié</option><option value="locked">Verrouillé</option></select></label><label>Objectifs, une ligne par objectif<textarea name="objectives" defaultValue={lesson.objectives.join("\n")} /></label></>}{tab === "content" && <label>Contenu Markdown<textarea className="content-editor" name="content" defaultValue={lesson.content ?? ""} /></label>}{tab === "resources" && <p className="env-note">Les ressources de leçon sont différées. Aucune source ou stockage n&apos;est modifié dans F2.1.</p>}<Button type="submit"><Save size={17} /> {pending ? "Sauvegarde..." : "Sauvegarder"}</Button>{message && <p className={message.includes("échoué") ? "form-error" : "completion-state"}>{message}</p>}</form>; }