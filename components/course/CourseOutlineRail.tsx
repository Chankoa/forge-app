"use client";
import Link from "next/link";
import { ChevronLeft, ChevronRight } from "lucide-react";
import { useState } from "react";
import type { CourseOutline } from "@/lib/courses/contracts";
import { IconButton } from "@/components/ui/IconButton";
export function CourseOutlineRail({ courseSlug, outline, selectedLesson, mode }: { courseSlug: string; outline: CourseOutline; selectedLesson?: string; mode?: "learn" | "edit" }) { const [expanded, setExpanded] = useState(true); const suffix = mode === "edit" ? "?mode=edit" : ""; return <aside className="outline-rail" aria-label="Parcours"><div className="rail-header">{expanded && <strong>Parcours</strong>}<IconButton label={expanded ? "Réduire le parcours" : "Ouvrir le parcours"} onClick={() => setExpanded(!expanded)}>{expanded ? <ChevronLeft size={18} /> : <ChevronRight size={18} />}</IconButton></div>{expanded && <ul className="outline-list">{outline.map((module) => <li className="outline-module" key={module.id}>{module.moduleTitle}{module.lessons.map((lesson) => <Link className={`outline-lesson outline-lesson--${lesson.status}`} aria-current={selectedLesson === lesson.slug ? "page" : undefined} href={`/app/courses/${courseSlug}/lessons/${lesson.slug}${suffix}`} key={lesson.id}>{lesson.status === "completed" && "✓ "}{lesson.title}</Link>)}</li>)}</ul>}</aside>; }