"use client";
import { ChevronLeft, ChevronRight, Sparkles } from "lucide-react";
import { useState } from "react";
import type { ForgeContext } from "@/lib/forge/contracts";
export function ForgeRail({ context }: { context: ForgeContext }) { const [expanded, setExpanded] = useState(true); const verb = context.mode === "learn" ? "comprendre" : "créer et améliorer"; const intents = context.mode === "learn" ? ["Expliquer", "Clarifier", "Reformuler", "Donner un exemple", "Me questionner"] : ["Structurer", "Améliorer", "Reformuler", "Simplifier", "Résumer", "Proposer des objectifs"]; return <aside className="forge-rail" aria-label="Forge"><div className="rail-header"><p className="eyebrow"><Sparkles size={15} /> Forge</p><button className="icon-button" aria-label={expanded ? "Réduire Forge" : "Ouvrir Forge"} onClick={() => setExpanded(!expanded)}>{expanded ? <ChevronRight size={18} /> : <ChevronLeft size={18} />}</button></div>{expanded && <><h2>Vous aider à {verb}</h2><p className="caption">Contexte : {context.lessonTitle ?? context.courseTitle}</p><div className="forge-intents">{intents.map((intent) => <button disabled key={intent}>{intent}</button>)}</div><p className="caption">Forge AI non configuré localement. Toute proposition nécessitera votre application explicite.</p></>}</aside>; }