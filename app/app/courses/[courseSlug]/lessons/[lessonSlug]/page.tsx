import { CourseWorkspace } from "@/components/course/CourseWorkspace";
import { resolveCourseCapabilities } from "@/lib/capabilities/course-capabilities";
const course = { id: "dev-fixture", slug: "fondations-forge", title: "Fondations de Forge", description: "Fixture DEV explicite.", domain: "Produit", status: "development" };
const outline = [{ moduleTitle: "Commencer", lessons: [{ slug: "une-intention", title: "Partir d'une intention" }, { slug: "un-espace", title: "Un seul espace" }] }];
export default async function LessonPage({ params }: { params: Promise<{ lessonSlug: string }> }) { const { lessonSlug } = await params; const lesson = outline[0].lessons.find((item) => item.slug === lessonSlug) ?? outline[0].lessons[0]; return <CourseWorkspace course={course} mode="learn" capabilities={resolveCourseCapabilities({ isOwner: true, isEnrolled: true })} outline={outline} selectedLesson={lesson.slug} forgeContext={{ mode: "learn", courseTitle: course.title, lessonTitle: lesson.title }} content={<><p className="eyebrow">FIXTURE DEV · Leçon</p><h2>{lesson.title}</h2><p>Le contenu reste dans le Course Workspace. Les capacités enrichiront cette même surface sans basculer vers une application distincte.</p></>} />; }