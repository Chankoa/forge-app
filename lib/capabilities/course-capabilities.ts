export type CourseCapabilities = { canView: boolean; canLearn: boolean; canPreview: boolean; canEdit: boolean; canPublish: boolean; canManageParticipants: boolean; };
export type CourseRelationship = { isOwner: boolean; isEnrolled: boolean; canManageParticipants?: boolean; };
export function resolveCourseCapabilities(relationship: CourseRelationship): CourseCapabilities { const isOwner = relationship.isOwner; return { canView: isOwner || relationship.isEnrolled, canLearn: relationship.isEnrolled, canPreview: isOwner, canEdit: isOwner, canPublish: isOwner, canManageParticipants: isOwner && Boolean(relationship.canManageParticipants) }; }
export function canAccessLessonMode(capabilities: CourseCapabilities, mode: string | undefined) { return mode === "edit" ? capabilities.canEdit : mode === "preview" ? capabilities.canPreview : capabilities.canLearn; }
